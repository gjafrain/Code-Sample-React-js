import { PRIMARY, WHITE } from "../../styles/colors";
import { padding } from "../../styles/mixins";

export default {
    container: {
        // ...padding(10, 10, 10, 10),
        // backgroundColor: PRIMARY
    },
    pageWrapper: {
        background: WHITE,
        display: 'flex'
    }
}