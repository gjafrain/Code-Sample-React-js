import { createAction } from "redux-actions";
import { constants } from "./constants";

// export const closeBasket = createAction(constants.CLOSE_BASKET_MODAL);
// export const openBasket = createAction(constants.OPEN_BASKET_MODAL);