import { handleActions } from "redux-actions";
import { initialState } from "./initialState";
// import { basketModalConstants } from "./constants";
// import { requestSuccess } from "../../../utils/fetch";

export const basketReducer = handleActions(
  {
   
  },
  initialState()
);
